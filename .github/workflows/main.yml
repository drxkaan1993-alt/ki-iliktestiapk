# Update this file to include the chmod +x command for gradlew

# This is the content of the main.yml file with the necessary update

# Other yaml contents may remain unchanged

- name: Grant execute permission for gradlew
  run: chmod +x gradlew
- name: Build
  run: ./gradlew build
